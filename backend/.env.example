# Provider keys belong on the backend only.
# Do not put AI provider keys in the desktop application.
# Never commit backend/.env.

ENABLE_AI_PROVIDER=false
AI_PROVIDER=openai
OPENAI_API_KEY=
AI_MODEL=gpt-4.1-mini
REQUEST_TIMEOUT=30
MAX_INPUT_SIZE=200000
MAX_OUTPUT_TOKENS=4096
LOG_LEVEL=INFO
